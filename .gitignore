# Python/Ambiente Virtual
#########################
venv/              # Ignora a pasta do ambiente virtual
__pycache__/       # Ignora a pasta de cache do Python
*.pyc              # Ignora arquivos compilados do Python
.env               # CRÍTICO: Ignora o arquivo de variáveis de ambiente (senhas, chaves)

# Django e Media
#########################
db.sqlite3         # Se você usar SQLite (mesmo que temporariamente)
/media/            # CRÍTICO: Pasta de uploads de arquivos (PDFs, Fotos). É grande e deve ser ignorada.
staticfiles/       # Arquivos estáticos gerados na produção (collectstatic)

# VS Code
#########################
.vscode/           # Pasta de configurações específicas do seu VS Code